package exemplo;
public class SemFacede {
    
    /*public static void main(String[] args) {
        VerificadorNumeroConta verificaNumConta = new VerificadorNumeroConta();
        VerificadorCodSeguranca verificaCodSeguranca = new VerificadorCodSeguranca();
        VerificadorSaldo verificaSaldo = new VerificadorSaldo();

        int numeroConta = 12345678;
        int codigoSeguranca = 1234;
        double valor = 100.0;

        if (verificaNumConta.contaAtiva(numeroConta) && verificaCodSeguranca.codCorreto(codigoSeguranca)) {
            if (verificaSaldo.sacarSaldo(valor)) {
                System.out.println("Saque realizado com sucesso!");
            } else {
                System.out.println("Erro: Saldo insuficiente!");
            }
        } else {
            System.out.println("Erro: Conta ou código de segurança incorretos!");
        }
    } */

}
